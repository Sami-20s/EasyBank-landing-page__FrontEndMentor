import React from "react";
import Header from "./Header/Header";
import Nav from "./Nav/Nav";

const Home = () => {
    return (
        <main>
            <Nav />
            <Header />
        </main>
    );
};

export default Home;
